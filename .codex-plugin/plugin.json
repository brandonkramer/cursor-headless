{
  "name": "cursor-headless",
  "version": "0.3.2",
  "description": "Delegate to Cursor Agent from Claude Code or Codex via MCP (ask/plan/implement). Claude workflows + /cursor-loop; slash commands for both hosts.",
  "license": "MIT",
  "keywords": ["cursor", "cursor-agent", "composer", "grok", "orchestration", "claude"],
  "skills": "./skills/",
  "commands": "./commands/",
  "mcpServers": {
    "cursor-headless": {
      "command": "uv",
      "args": [
        "run",
        "--with",
        "mcp>=1.9,<2",
        "--with",
        "cursor-sdk",
        "--python",
        "3.14",
        "python",
        "src/cursor_headless_mcp.py"
      ],
      "cwd": ".",
      "env": {
        "PYTHONUTF8": "1",
        "PYTHONIOENCODING": "utf-8"
      }
    }
  },
  "interface": {
    "displayName": "Cursor Headless",
    "shortDescription": "Native Cursor ask/plan/implement tools for Codex and Claude",
    "longDescription": "Thin MCP facade over cursor-agent --print. cursor_ask/plan default to cursor-grok-4.5-high; cursor_implement defaults to composer-2.5 — pick Grok low/medium/high and whether to use Fast. Works as a Codex plugin and a Claude Code plugin.",
    "category": "Developer Tools",
    "capabilities": ["Read", "Write"],
    "defaultPrompt": [
      "Use cursor_ask with Grok 4.5 low/medium/high (opt into Fast) by complexity.",
      "Use cursor_plan with Grok 4.5 low/medium/high (opt into Fast) by complexity.",
      "Use cursor_implement with composer-2.5 (opt into Fast) or Grok 4.5 low/medium/high by complexity.",
      "You (parent) set timeout per call (default 1200s). Raise for broad maps; on timeout treat as no result and narrow or raise timeout.",
      "Read progress_summary + job_id from cursor_* envelopes; poll cursor_status(job_id) when parallel tools are available.",
      "Default backend cli; MCP uv env includes cursor-sdk — set CURSOR_API_KEY + CURSOR_HEADLESS_BACKEND=sdk or backend=sdk to use SDK.",
      "On Windows prefer MCP cursor_* tools (UTF-8) over shelling cursor-agent into a CP-1252 console.",
      "Use /cursor-implement to fan out parallel Cursor workers from Codex.",
      "Use /cursor-review-loop for parent review → Cursor fix → re-review until clean.",
      "On Claude Code, use /cursor-loop to arm /loop with cursor-headless implement/review/babysit ticks."
    ]
  }
}
