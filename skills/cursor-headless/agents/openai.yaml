interface:
  display_name: "Cursor Headless"
  short_description: "MCP tools cursor_ask / cursor_plan / cursor_implement"
  default_prompt: "For cursor_ask/plan pick Grok 4.5 low/medium/high and whether to use Fast (default high, non-Fast). For cursor_implement default composer-2.5; opt into Fast or escalate to Grok by complexity. Parent sets timeout per call (default 1200s); on timeout treat as no result and narrow or raise timeout."
